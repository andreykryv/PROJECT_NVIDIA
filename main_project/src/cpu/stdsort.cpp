////////////////////////////////////////////////////////////////////////////////
// cpu/stdsort.cpp — реализация обёртки std::sort
// Вызывает std::sort(data, data+size). После завершения испускает один кадр.
// Явные инстанции: int32_t, int64_t, float, double.
////////////////////////////////////////////////////////////////////////////////
