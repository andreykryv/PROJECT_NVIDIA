////////////////////////////////////////////////////////////////////////////////
// ui/progresspanel.cpp — реализация панели прогресса
//
// СОДЕРЖИМОЕ:
//   Конструктор создаёт QVBoxLayout с progressBar и тремя горизонтальными
//   строками меток. Запускает QTimer для обновления elapsedLabel каждые 100мс.
//
// ЛОГИКА cpuGpuSplitBar::paintEvent():
//   — Получает пропорцию: ratio = cpuMs / (cpuMs + gpuMs).
//   — Рисует синий прямоугольник шириной ratio*width и зелёный — остаток.
//   — Подписи центрируются внутри своих прямоугольников.
//   — При cpuMs == 0 и gpuMs == 0: рисует серый прямоугольник "Нет данных".
//   — Тонкая белая/чёрная граница между участками для лучшей читаемости.
//
// ЛОГИКА отображения скорости:
//   — speed = arraySize / elapsedSeconds (элементов в секунду).
//   — Форматирование: < 1M → "K/s", 1M–1G → "M/s", > 1G → "G/s".
////////////////////////////////////////////////////////////////////////////////
