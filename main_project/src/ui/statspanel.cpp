////////////////////////////////////////////////////////////////////////////////
// ui/statspanel.cpp — реализация панели статистики
//
// СОДЕРЖИМОЕ:
//   Конструктор создаёт QVBoxLayout с тремя QGroupBox.
//   updateResult() заполняет все метки из BenchmarkResult.
//
// ФОРМАТИРОВАНИЕ УСКОРЕНИЯ:
//   — speedup > 10x: ярко-зелёный, жирный шрифт
//   — speedup 2–10x: зелёный
//   — speedup 1–2x: жёлтый (GPU слегка быстрее)
//   — speedup < 1x: красный (GPU медленнее CPU)
//   — Цвета задаются через setStyleSheet("color: #...") динамически.
//
// ИСТОРИЯ:
//   — Добавляет новый QListWidgetItem при каждом updateResult().
//   — При превышении maxHistory (30) удаляет самый старый элемент.
//   — Хранит BenchmarkResult в QListWidgetItem::setData(Qt::UserRole).
////////////////////////////////////////////////////////////////////////////////
