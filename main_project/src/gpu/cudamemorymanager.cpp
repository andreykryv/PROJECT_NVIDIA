////////////////////////////////////////////////////////////////////////////////
// gpu/cudamemorymanager.cpp — реализация RAII-обёрток памяти CUDA
//
// Реализует конструкторы/деструкторы всех RAII-классов.
// Все CUDA-вызовы обёрнуты в CUDA_CHECK. При ошибке выбрасывается
// std::runtime_error с описанием cudaGetErrorString(err).
// Явные инстанции DeviceBuffer<T> и PinnedBuffer<T>: int32_t, int64_t, float, double.
////////////////////////////////////////////////////////////////////////////////
