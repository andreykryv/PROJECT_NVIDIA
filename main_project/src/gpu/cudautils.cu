////////////////////////////////////////////////////////////////////////////////
// gpu/cudautils.cu — реализация device-утилит (компилируется nvcc)
//
// Большинство функций cudautils.cuh — inline __device__, не требуют отдельного .cu.
// Этот файл содержит:
//   — Non-inline __global__ ядра (exclusiveScanBlock)
//   — Тесты корректности отдельных утилит (ifdef SORTBENCH_TEST)
//   — Инициализационный код: cudaFuncSetCacheConfig для всех utility-ядер
////////////////////////////////////////////////////////////////////////////////
